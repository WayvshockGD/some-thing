const os = require('os');

class utils
{
    constructor()
    {
        super("utilFile")
        {
            module.exports.types = {
                type: "node",
                operating: ["win32", "win64"],
            }

            if(os.type === "win32" || "win64")
            {
               for (const os of type)
               {
                   var functions = {};
                   var task = {};

                   array.forEach(element => {
                       if(!os) 
                       {
                           return;
                       }

                       this.updateLocale = task && functions
                   });
               }
            }
        }
    }
}

module.export = utils